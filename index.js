// CHALLENGE: Create a function that adds two numbers together using modern Arrow Function syntax.

const addNumbers = (a, b) => {
    return a + b;
};

// Test log to verify logic works
console.log(addNumbers(5, 10)); // Output should be 15